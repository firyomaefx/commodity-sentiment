services:
  - type: web
    name: commodity-sentiment-bot
    runtime: static
    buildCommand: "echo '<h1>Archived</h1><p>Bot archived. Dashboard-only mode.</p>' > index.html"
    staticPublishPath: .
    envVars: []
